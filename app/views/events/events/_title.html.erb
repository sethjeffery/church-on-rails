<%= render 'shared/title',
           icon: :calendar,
           title: (local_assigns[:title] || @event),
           short: local_assigns[:short],
           color: (local_assigns[:color] || :event) %>
