<%= render 'shared/title',
           icon: :calendar,
           align: :center,
           title: (local_assigns[:title] || @event),
           short: local_assigns[:short],
           background: (local_assigns[:background] || :event) %>
