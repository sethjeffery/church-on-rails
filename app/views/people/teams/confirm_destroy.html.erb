<%= render 'menu', active: :destroy, title: 'Remove' do %>
  <div class="alert alert-info py-2 text-xs-center">
    <p>
      If you do this, <strong><%= @team %></strong> will be removed from the database.
      Any members will stay in the database but no longer a part of this team.
    </p>

    <p class="size-xs-large">
      Are you sure?
    </p>

    <div class="container-xs">
      <%= button_to 'Yes, do it.', @team, method: :delete, class: 'btn btn-danger btn-block btn-lg' %>
    </div>
  </div>
<% end %>
