<%= render 'shared/title',
           title: (local_assigns[:title] || @person.to_s),
           color: local_assigns[:color],
           short: local_assigns[:short],
           icon: local_assigns[:icon] %>
