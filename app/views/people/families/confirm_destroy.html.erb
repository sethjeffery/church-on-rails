<%= render 'people/families/title', background: :danger %>

<div class="container-xs">
  <div class="list-group mb-2">
    <div class="list-group-item active">
      Remove family
    </div>

    <div class="list-group-item">
      <p>
        If you do this, <strong><%= @family %></strong> will be removed from the database.
        Any family members will remain in the database but will not be connected to a family.
        Are you sure?
      </p>

      <%= link_to 'No, stop!', @family, class: 'btn btn-secondary btn-block btn-lg mb-sm' %>
      <%= button_to 'Yes, do it.', @family, method: :delete, class: 'btn btn-danger btn-block btn-lg mb-sm' %>
    </div>
  </div>
</div>
