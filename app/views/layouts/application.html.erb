<!DOCTYPE html>
<html lang="en">
  <head>
    <%= render 'layouts/head' %>
  </head>

  <body class="<%= 'with-side-menu' if side_menu? %>">
    <% if side_menu? %>
      <%= render 'shared/side_menu' %>
    <% end %>

    <div class="container py-1">
      <%= yield %>
    </div>
  </body>
</html>
