<h2 class="text-xs-center mb-1">Families</h2>

<%= list_group @families do |family| %>

  <div class="row">
    <div class="col-sm-6">
      <%= link_to family, class: 'text-ellipsis' do %>
        <%= family.name %>
      <% end %>
    </div>
    <div class="col-sm-6">
      <div class="text-ellipsis text-light size-xs-small">
        <%= family.people.map(&:short_name).join(', ') %>
      </div>
    </div>
  </div>

<% end %>
