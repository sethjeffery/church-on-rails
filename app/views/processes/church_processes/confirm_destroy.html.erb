<%= render 'menu', active: :destroy, title: 'Remove process' do %>
  <div class="alert alert-info py-2 text-xs-center">
    <p>
      If you do this, <strong><%= @church_process %></strong> will be removed from the database.
    </p>

    <p class="size-xs-large">
      Are you sure?
    </p>

    <div class="container-xs">
      <%= button_to 'Yes, do it.', @church_process, method: :delete, class: 'btn btn-danger btn-block btn-lg' %>
    </div>
  </div>
<% end %>
