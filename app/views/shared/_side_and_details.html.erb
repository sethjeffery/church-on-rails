<div class="container">
  <div class="side-and-details mt-1">
    <div class="side-and-details--details">
      <% if local_assigns[:title] %>
        <h2>
          <% if local_assigns[:title].is_a?(Person) %>
            <%= avatar_tag local_assigns[:title], class: 'avatar-md float-xs-right' %>
          <% end %>
          <%= title %>
        </h2>
      <% end %>
      <%= yield %>
    </div>

    <div class="side-and-details--side">
      <ul class="nav nav-stacked">
        <% items.compact.each do |item| %>
          <% if item.blank? || item == '-' %>
            <li class="nav-divider"></li>
          <% else %>
            <li class="nav-item">
              <%= link_to item[:href], class: "nav-link #{'active' if item[:active] || local_assigns[:active] == item[:icon]}" do %>
                <div class="fa-container">
                  <%= icon_for item[:icon] %>
                  <%= item[:title] %>
                </div>
              <% end %>
            </li>
          <% end %>
        <% end %>
      </ul>
    </div>
  </div>
</div>
